const { UserModel } = require('../schemas/user');

class User {
    static async getUserInfo({}) {
        return
    }

    static async create({}) {
        return
    }

    static async update({}) {
        return
    }

    static async delete({}) {
        return
    }
}

module.exports = User